import { getSubAdminByCounty } from '@/services/adminService';

export async function getSubAdminByCounty(countyId) {
  // replace with real API call later
  // return { name: 'Placeholder', email: 'admin@example.com' } or null
  return {
    id: 0,
    name: 'No sub-admin',
    email: ''
  };
}